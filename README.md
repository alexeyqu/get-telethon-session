# get-telethon-session

## Usage

* `python -m pip install -r requirements.txt`

* Replace `api_id` and `api_hash` in code

* `python get_telethon_session.py`

* copy the entire session string and use it